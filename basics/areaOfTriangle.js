/**
 * 
 * @param {Number} w Widht of triangle
 * @param {Number} h Height of triangle
 * @returns Number Area of the triangle based on height and Widht
 */
export function areaofTriangle(w, h) {
    return 0.5 * w * h;
}

