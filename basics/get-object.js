export function getObject(name, age, city, experience, contact) {
    let obj = {
        name: name,
        age: age,
        city: city,
        exp: experience,
        contc: contact,

    }

    return console.log('Name', obj.name, 'City', obj.city);

}
